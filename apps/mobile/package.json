{
  "name": "@jessmove/mobile",
  "version": "1.0.0",
  "private": true,
  "description": "The native shell — Apple Health, Health Connect, the device calendar and motion, wrapped around the existing web app",
  "license": "UNLICENSED",
  "scripts": {
    "sync": "cap sync",
    "add:ios": "cap add ios",
    "add:android": "cap add android",
    "open:ios": "cap open ios",
    "open:android": "cap open android",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/push-notifications": "^8.0.0",
    "@jessmove/shared": "workspace:*"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.6.3"
  }
}
